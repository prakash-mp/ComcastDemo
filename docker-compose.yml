version: '3.8'

services:
  comcast-backend:
    image: 'comcast-backend:${TAG-latest}'
    build:
      dockerfile: .dockerfile
    ports:
      - "8081:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
#    depends_on:
#      - db
